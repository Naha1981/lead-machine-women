name: Demo E2E

on:
  pull_request:
    paths:
      - "src/**"
      - "scripts/smoke-demo.mjs"
      - "package.json"
      - "bun.lock"
      - ".github/workflows/demo-e2e.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_APP_URL: http://127.0.0.1:3000
      NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_test_ZXhhbXBsZS5hY2NvdW50cy5kZXYk
      NEXT_PUBLIC_APP_ID: lead-machine
      ENABLE_DEMO_MODE: "true"
      SIMULATE_WHATSAPP: "true"
      CRON_SECRET: smoke-secret
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - run: bun install --frozen-lockfile
      - name: Start development server
        run: |
          nohup bun run dev > /tmp/lead-machine-dev.log 2>&1 &
          echo $! > /tmp/lead-machine.pid
      - name: Wait for server
        run: |
          sleep 8
          curl --silent --show-error --fail --max-time 5 http://127.0.0.1:3000/api/health >/dev/null
      - name: Run dentist end-to-end smoke test
        env:
          BASE_URL: http://127.0.0.1:3000
        run: bun scripts/smoke-demo.mjs
      - name: Server log on failure
        if: failure()
        run: cat /tmp/lead-machine-dev.log
