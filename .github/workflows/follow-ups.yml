name: Lead Machine follow-ups

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger follow-up worker
        env:
          APP_URL: ${{ secrets.LEAD_MACHINE_APP_URL }}
          CRON_SECRET: ${{ secrets.LEAD_MACHINE_CRON_SECRET }}
        run: |
          test -n "$APP_URL"
          test -n "$CRON_SECRET"
          curl --fail --silent --show-error \
            -H "Authorization: Bearer $CRON_SECRET" \
            "$APP_URL/api/cron/follow-ups"
